/**
 * Structured runtime state export for AI coding agents.
 *
 * <p>Produces machine-readable JSON describing the current JVM state.
 * Designed for LLM consumption: bounded output, deterministic key
 * ordering, per-section error isolation, and security-filtered
 * properties. Zero external dependencies.
 */
package jdk.aprismate.export;
